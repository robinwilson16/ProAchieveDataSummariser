DECLARE @AcademicYear VARCHAR(5) = '18/19'
DECLARE @CollegeType INT = 0
DECLARE @Mode CHAR(1) = 'R'
DECLARE @ProGeneralDatabaseLocation VARCHAR(200) = 'ussql01.ProGeneral.dbo.'
DECLARE @ProAchieveDatabaseLocation VARCHAR(200) = 'ussql01.ProAchieve.dbo.'
DECLARE @OutputTableLocation VARCHAR(200) = 'ProSolutionReports.dbo.'

DECLARE @SQL VARCHAR(MAX);

SET @SQL = '
	EXEC SPR_PRA_GenerateProAchieveSummaryData
		''' + @AcademicYear  + ''', 
		''' + CAST ( @CollegeType AS VARCHAR(6) ) + ''', 
		''' + @Mode + ''', 
		''' + @ProGeneralDatabaseLocation + ''',
		''' + @ProAchieveDatabaseLocation + ''',
		''' + @OutputTableLocation + ''''

EXEC(@SQL)