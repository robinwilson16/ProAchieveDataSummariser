DECLARE @NumRowsInserted INT
DECLARE @Error NVARCHAR(255)

EXEC SPR_RefreshProAchieveLearnerData 
	'17/18', 
	2,
	@NumRowsInserted OUTPUT, 
	@Error OUTPUT

IF LEN(@Error) > 0
	PRINT N'Error: ' + @Error
ELSE
	PRINT N'Rows inserted: ' + CAST ( @NumRowsInserted AS NVARCHAR(10) )